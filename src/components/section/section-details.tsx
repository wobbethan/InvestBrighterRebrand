import React from "react";
import LinkSnippet from "./link-snippet";
import { Section } from "@prisma/client";

type Props = {
  section: Section;
};

const SectionDetails = ({ section }: Props) => {
  return <div></div>;
};

export default SectionDetails;
